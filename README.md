# Foveated-Imaging-Matlab

Manipulating array of images using matlab to control blur level on specific image coordinates (row & columns) in order to make the image foveated at one particuler location
